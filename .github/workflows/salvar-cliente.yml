
name: Atualizar clientes

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  atualizar:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Verificar arquivos
        run: |
          echo "Arquivos encontrados:"
          ls

      - name: Preparado para atualizar clientes.json
        run: |
          echo "Workflow funcionando"
